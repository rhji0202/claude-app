{
  "name": "claude-management",
  "version": "0.1.0",
  "private": true,
  "description": "Claude 관리 시스템 모노레포 (NestJS API + Next.js UI + 공유 타입)",
  "scripts": {
    "dev": "pnpm --parallel -r dev",
    "dev:api": "pnpm --filter @claude-app/api start:dev",
    "dev:web": "pnpm --filter @claude-app/web dev",
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck",
    "prisma:generate": "pnpm --filter @claude-app/api prisma:generate",
    "prisma:migrate": "pnpm --filter @claude-app/api prisma:migrate"
  },
  "devDependencies": {
    "concurrently": "^9.1.0",
    "typescript": "^5.7.0"
  },
  "engines": {
    "node": ">=20"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "@prisma/client",
      "@prisma/engines",
      "prisma",
      "esbuild",
      "sharp"
    ]
  }
}
